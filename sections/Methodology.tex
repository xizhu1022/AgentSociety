\section{A Tokenomics-Driven Ecosystem for Agent Society}
\label{sec:ecosystem}

This section outlines the concrete design of our tokenomics-driven decentralized ecosystem for the agent society. We first formalize the agent roles and knowledge service abstractions that form the architecture's foundation (\S\ref{sec:motivation}). Building on this basis, we describe the core interaction primitives, namely promotion, subscription, and validation, through which agents engage with one another (\S\ref{sec:mechanism}). Finally, we detail the tokenomics incentive structure, incorporating staking, rewards, and penalties, to ensure the entire ecosystem remains self-enforcing and economically sustainable (\S\ref{sec:tokenomics_loop}).


\subsection{Agent Roles and Agent Services}
\label{sec:motivation}

In the agent society, specialized intelligence is distributed across a large-scale, open population of autonomous agents, where each agent possesses domain-specific intelligence composed of its trained knowledge, reasoning capabilities, and retrieval resources, and operates under the authority of its deploying organization. When an agent encounters a task outside its own competence, it must locate and engage a capable counterpart, a process we term agent service routing. Rather than delegating this task to a centralized orchestrator, our ecosystem allows routing to emerge organically from token-mediated economic interactions among three dynamically interchangeable roles.

For any agent $u \in \mathcal{U}$ with domain-specific expertise $\mathcal{K}_u$, the most immediate roles arise from the service supply-demand structure.

% In the agent society, specialized knowledge is distributed across a large, open population of autonomous agents $\mathcal{U}$, each possessing domain-specific expertise $\mathcal{K}_u$ and operating under the authority of its deploying organization. When an agent encounters a task that falls outside its own competence, it must locate and engage a capable counterpart---a process we term \textit{service routing}. Rather than delegating this routing to a centralized orchestrator, our ecosystem allows it to emerge organically from token-mediated economic interactions among three dynamically interchangeable roles.

\begin{definition}{\textbf{(Consumer Agent)}}\label{def:consumer}
A consumer $c \in \mathcal{U}$ is an agent that requests services from the ecosystem. Upon encountering a task outside its domain expertise, it identifies the required expertise domain and issues a set of domain-specific tasks $\{x_1, \ldots, x_{N}\} \subset \mathcal{Q}$ to be delegated externally. A consumer agent seeks to establish a long-term service agreement with a qualified producer agent, committing tokens to fund this engagement. Unlike a human user who subjectively "asks a question," the consumer agent executes a deterministic routing protocol driven by its utility function.
\end{definition}

\begin{definition}{\textbf{(Producer Agent)}}\label{def:producer}
A producer $p \in \mathcal{U}$ is an agent that provides agent services by encapsulating its domain expertise $\mathcal{K}_p$ into a callable inference capability. Given a task $x \in \mathcal{Q}$, the producer executes its service $a_p(x)$ and returns an output $r \in \mathcal{R}$. The service quality $q_p \in (0,1]$, representing the probability of generating a correct output, is treated as an objective property of the agent that can be estimated through sampling. By delivering high-quality outputs, the producer agent earns tokens from the consumer agent.
\end{definition}

As established in \Cref{sec:trust_crisis}, the consumer-producer dyad alone cannot sustain a reliable agent service market. The information asymmetry between supply and demand, where the consumer lacks the means to verify service quality and the producer may overstate its competence, triggers the adverse selection that our mechanism must overcome. To break this asymmetry and move beyond the risks of centralized "black-box" routing, we introduce a third role: validators, independent agents that provide decentralized quality assessments of agent service on behalf of the ecosystem.

\begin{definition}{\textbf{(Validator Agent)}}\label{def:validator}
A validator $v \in \mathcal{U}$ is an independent agent that performs cross-verification within the agent service routing process. Given a task $x \in \mathcal{Q}$ and a producer's output $a_p(x)$, the validator agent independently executes its own inference $a_v(x)$ to produce a judgment $j_v \in \{0, 1\}$, indicating agreement or disagreement. This is an auditable computational process rather than a subjective evaluation. To enhance reliability, judgments from multiple validators can be aggregated to form a consensus that ensures the integrity of the routing results (see \S\ref{sec:poi_detail}).
\end{definition}

Central to both roles is the concept of domain-specific and intelligence-intensive agent services. A producer contributes to the ecosystem through expected long-term service engagements by fulfilling recurring consumer requests, while a validator supports the agent service routing process by independently assessing the quality of producer-consumer matching beforehand. Although their purposes differ, one generating responses and the other verifying them, both agents rely on the same underlying inference capability to provide their respective services.

\begin{definition}{\textbf{(Agent Service Routing)}}\label{def:agent-service-routing}
Each validator $v \in \mathcal{U}$ encapsulates its domain expertise $\mathcal{K}_v$ into a knowledge-intensive routing service $a_v: \mathcal{Q} \rightarrow \mathcal{R}$, providing a reference inference to determine a producer's reliability. From its own perspective, the validator utilizes $a_v$ to assess a producer's performance on a specific task, where $q \in (0,1]$ reflects its internal confidence in the verification's accuracy. This process incurs a computational cost $c_v$ per invocation. Ultimately, the validator functions as a decentralized benchmark, leveraging its proprietary inference pipeline to participate in the service routing between consumers and producers.
\end{definition}

% It is worth noting that $a_u$ exhibits a functional duality depending on the agent's role in the operational context. When $u$ acts as a producer, $a_u$ functions as the primary solution, delivering direct utility to the consumer by resolving their specific task. Conversely, when $u$ acts as a validator, the same $a_u$ serves as an independent benchmark, not to solve the consumer's problem directly, but to provide an epistemic reference for assessing the producer’s performance. 

% This symmetric design implies that any agent with sufficient domain expertise can switch between production and validation, ensuring that the ecosystem's capacity for quality control scales naturally with its capacity for service delivery.

Conceptually, an agent’s inference capability constitutes a non-transferable asset inherently bound to its owner, shaped by its unique training data, reasoning architecture, and retrieval resources. This service quality represents the agent’s core competitive advantage, dictating both its earning potential as a producer and its credibility as a validator. Specifically, this capability exhibits a functional duality defined by its operational context: as a producer, the agent employs its expertise as the primary solution to deliver direct utility to the consumer; conversely, as a validator, it utilizes the same service as an independent benchmark to provide an epistemic reference for assessing performance. Within this two-sided market, agent intelligence is the traded commodity, while tokens serve as the medium of exchange that aligns individual incentives with ecosystem-wide welfare.

\begin{insightbox}{Agent Roles in the Ecosystem}
\begin{itemize}[leftmargin=1.5em, itemsep=3pt, topsep=2pt]
    \item \textbf{Consumer agent} --- identifies tasks beyond its expertise and delegates them externally via token-backed service requests.
    \item \textbf{Producer agent} --- encapsulates domain expertise into a callable agent service $a_p$, expecting to earn tokens for continuous reliable outputs.
    \item \textbf{Validator agent} --- provides an independent routing service $a_v$ that benchmarks the producer’s output, contributing a judgment to the ecosystem’s consensus.
\end{itemize}
\vspace{1pt}
\textit{Role Fluidity}: Any agent may assume any role based on its context and expertise. The same agent may act as a producer in one domain, a consumer in another, and a validator in a third.
\end{insightbox}

\subsection{Agent Actions and Validation Protocol}
\label{sec:mechanism}

Building on the agent roles and agent service abstractions defined above, the ecosystem coordinates agent interactions through three core actions: promotion, validation, and subscription. Each action is underpinned by token commitments designed to maintain the integrity of the agent society. This subsection traces the lifecycle of a typical agent service engagement, moving from initial need identification through to confirmed subscription. For reference, Table~\ref{tab:notation} summarizes the key notation used throughout the paper.

\begin{table}[!htbp]
\centering
\caption{Summary of notation used throughout the paper.}
\vspace{2mm}
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1.20}
\small
\begin{tabular}{cl}
\toprule
\multicolumn{2}{c}{\textbf{Agents and Roles}} \\
\midrule
$\mathcal{U}$ & Population of all agents in the ecosystem \\
$c,\; p,\; v$ & Consumer, producer, and validator agent \\
$\mathcal{K}_u$ & Domain-specific expertise of agent $u$ (knowledge, reasoning, retrieval) \\
\midrule
\multicolumn{2}{c}{\textbf{Agent Services}} \\
\midrule
$a_u\!:\! \mathcal{Q} \!\to\! \mathcal{R}$ & Domain-specific agent service (inference capability) of agent $u$ \\
$\mathcal{Q}$ & Task space (set of all possible task instances) \\
$\mathcal{R}$ & Response space (set of all possible outputs) \\
$x,\; x_j$ & Individual task instance ($x \in \mathcal{Q}$) \\
$q_p,\; q_v$ & Service quality of producer / validator (correctness probability) \\
$c_u$ & Computational cost per invocation of $a_u$ \\
\midrule
\multicolumn{2}{c}{\textbf{Tokenomics Parameters}} \\
\midrule
$\tau_p$ & Promotion fee staked by the producer agent \\
$\tau_s$ & Subscription fee paid by the consumer agent \\
$\tau_{\text{val}}$ & Validation fee per task per validator agent \\
$\sigma$ & Token stake locked by each validator during validation \\
\midrule
\multicolumn{2}{c}{\textbf{Validation and Consensus}} \\
\midrule
$m$ & Number of sample tasks per validation round \\
$n$ & Number of validator agents per committee (odd) \\
$V_p,\; V_s$ & Validator committees for promotion and subscription \\
$r_i,\; r_j'$ & Judgment of validator $v_i$ / $v_j'$ ($\in \{0,1\}$) \\
$r^*,\; r^{**}$ & Majority-vote consensus for promotion / subscription \\
$Z \in \{0,1\}$ & Consensus signal observed by the requester (pass / fail) \\
\midrule
\multicolumn{2}{c}{\textbf{Game-Theoretic Quantities}} \\
\midrule
$P_{\text{match}}(n, q_v)$ & Probability that a truthful validator's vote matches the majority \\
$R_{\min}$ & Conservative per-capita validator reward ($= \tau_{\text{val}} + \sigma$) \\
$V_p(q_p)$ & Probability that a producer of quality $q_p$ passes validation \\
$\bar{q}_p,\; \bar{q}_c$ & Quality thresholds for promotion and subscription \\
\bottomrule
\end{tabular}
\label{tab:notation}
\end{table}


\subsubsection{Promotion and Subscription}

The agent service lifecycle revolves around two complementary economic actions that together constitute the two-sided market at the heart of the ecosystem. Producers promote their agent services to establish visibility and attract potential users, while consumers subscribe to preferred producers for long-term agent service. These two workflows are illustrated in Figures~\ref{fig:promotion} and~\ref{fig:subscription}.

\begin{definition}{\textbf{(Promotion)}}\label{def:promotion}
A producer agent $p$ initiates a promotion by advertising its service capabilities and staking a commitment fee $\tau_p$. This stake functions as a costly signal, ensuring that only agents with high confidence in their competence will risk the potential forfeiture of tokens to establish market visibility.
\end{definition}

\begin{definition}{\textbf{(Subscription)}}\label{def:subscription}
A consumer agent $c$ establishes a \textit{subscription} to a producer's service $a_p$ by paying a fee $\tau_s$ in tokens, forming a service-level agreement (SLA) between the two autonomous agents. Under this agreement, the consumer delegates future tasks to $a_p$, and the producer becomes obligated to respond within specified quality and latency parameters. 
\end{definition}

% \begin{definition}{\textbf{(Promotion)}}\label{def:promotion}
% A producer agent $p$ initiates a \textit{promotion} by publishing a verifiable performance declaration---comprising its domain coverage, inference capability profile, and willingness to be evaluated---backed by a token stake $\tau_p$. This stake functions as a costly signal~\cite{spence1973job}: an agent lacking genuine competence faces a high probability of failing the subsequent validation and forfeiting $\tau_p$, rendering spurious declarations economically irrational. Promotion thus serves as a decentralized entry filter that screens out underqualified producers without centralized gatekeeping.
% \end{definition}

% \begin{definition}{\textbf{(Subscription)}}\label{def:subscription}
% A consumer agent $c$ establishes a \textit{subscription} to a preferred producer's service $a_p$ by committing a subscription fee $\tau_s$ in tokens, forming a service-level agreement (SLA) between the two autonomous agents. Under this agreement, the consumer delegates future tasks to $a_p$, and the producer becomes obligated to respond within specified quality and latency parameters. A mismatched subscription carries significant consequences: the consumer wastes its fee and suffers degraded downstream performance, with cascading effects across any agent systems that depend on its outputs.
% \end{definition}

\begin{insightbox}{Agent Service Lifecycle at a Glance}
\begin{itemize}[leftmargin=1.5em, itemsep=3pt, topsep=2pt]
    \item \textbf{Promotion} (supply side). A producer agent stakes $\tau_p$ to publicly declare its service capability, putting itself forward as a candidate provider for interested consumers.
    \item \textbf{Subscription} (demand side). A consumer agent commits $\tau_s$ to a chosen producer, establishing a long-term service engagement under agreed quality.
\end{itemize}
\vspace{1pt}
\textit{Key property}: Both promotion and subscription are high-stakes, token-backed commitments, creating a natural demand for third-party validations before finalizing decisions. 
\end{insightbox}

In practice, promotion and subscription are high-stakes, long-term commitments involving substantial token expenditure. Because poor matches erode trust and undermine incentive alignment, rational agents have a strong incentive to seek independent quality assessments before finalizing any agreement. This demand for transparency and reliability is precisely what the validation protocol addresses.

\subsubsection{Validation Requests}

% In conventional platforms, agent routing relies heavily on subjective judgment, where producers evaluate their own competence and consumers rely on intuition to judge results. However, objective ground truth is often unavailable because consumers lack the expertise to verify correctness, while producers operate based on partial or biased knowledge. To bridge this gap, the ecosystem provides a structured third-party validation mechanism that allows heterogeneous agents to reach an independent consensus on service quality. Since promotion and subscription are high-stakes, long-term commitments, both parties are motivated to invoke this cost-efficient validation to mitigate mismatch risks before finalizing any transaction.

% In conventional platforms, consumer-producer routing relies heavily on subjective judgments. Producers evaluate their own competence and provide answers they believe to be correct, while consumers rely on intuition to judge the responses they receive. However, objective ground truth is usually unavailable: consumers often lack expertise to verify correctness, and producers answer based on their partial or biased knowledge. 

% To address this issue, heterogeneous third-party validators make independent decision and reach consensus, yielding outcomes that should be more objective and reliable. Since promotion and subscription are long-term commitments with substantial costs, both producers and consumers have motivation to request cost-efficient validation to reduce potential mismatch risks.

In conventional systems, producer-consumer agent routing relies heavily on subjective judgment, where producers evaluate their own competence and consumers rely on intuition to judge results. However, objective ground truth is often unavailable because consumers usually lack the expertise to verify correctness, while producers may operate based on partial or biased knowledge. To bridge this gap, the ecosystem provides a structured third-party validation mechanism that both producers and consumers can request before committing to high-stakes transactions.

% Given that agent service quality cannot be unilaterally verified by the consumer, the ecosystem provides a structured third-party validation mechanism that both producers and consumers can invoke before committing to high-stakes transactions. By design, the total cost of validation ($m \times n \times \tau_{\text{val}}$) is substantially lower than that of a mismatched subscription ($\tau_s$), making the request for validation economically rational for both parties. The protocol operates in two stages, each employing an independent validator committee to ensure integrity.

\begin{definition}{\textbf{(Promotion Validation Request)}}\label{def:promo_validation}
When a consumer agent $c$ broadcasts a service need along with a sample task set $\mathcal{Q}_c \subset \mathcal{Q}$ ($|\mathcal{Q}_c| = m$), a producer agent $p$ requests a group of validators $V_p \subset \mathcal{U}$ ($|V_p| = n$) and pays each validator a fixed fee $\tau_{\text{val}}$ per task to assess its service capability before initiating a formal promotion.
\end{definition}

\begin{definition}{\textbf{(Subscription Validation Request)}}\label{def:sub_validation}
Upon receiving a promotion, the consumer agent $c$ issues a separate task set $\mathcal{Q}_s \subset \mathcal{Q}$ ($|\mathcal{Q}_s| = m$) to a disjoint group of validators $V_s \subset \mathcal{U}$ ($|V_s| = n$), subject to the constraints $\mathcal{Q}_s \cap \mathcal{Q}_c = \varnothing$ and $V_s \cap V_p = \varnothing$. Each validator in $V_s$ is paid the same per-task fee $\tau_{\text{val}}$ to independently assess the producer's service quality.
\end{definition}

By design, the total cost of validation $m \cdot n \cdot \tau_{\text{val}}$ is substantially lower than the potential loss from a mismatched subscription $\tau_s$, where $m$ and $n$ denote the sizes of the task set $\mathcal{Q}_c$ (or $\mathcal{Q}_s$) and the validator committee $V_p$ (or $V_s$) respectively. Meanwhile, the per-task fee is structured to exceed each validator’s computational cost $c_v < \tau_{\text{val}}$. This makes validation a cost-efficient safeguard: rational agents are incentivized to invoke the protocol because the modest upfront expenditure mitigates the far greater risks of biased self-assessment and long-term commitment failure. To maintain systemic integrity, the protocol operates in two distinct stages, each employing an independent validator committee to prevent collusion and ensure objectivity.


\begin{figure*}[!tbp]
\centering
\vspace{-4mm}
\includegraphics[width=\textwidth]{figures/Promotion.pdf}
\vspace{-2mm}
\caption{Overall Promotion Workflow.
(1)~A consumer broadcasts a sample task when a service need arises.
(2)~A producer provides its output and requests validators to assess correctness.
(3)~Validators independently produce outputs, form a consensus, and return it to the producer.
(4)~If the producer's output agrees with the consensus, the service capability is confirmed, and the producer proceeds with the promotion offer to the consumer.}
\label{fig:promotion}
\vspace{-4mm}
\end{figure*}


\begin{figure*}[!htbp]
\centering
\includegraphics[width=\textwidth]{figures/Subscription.pdf}
\vspace{-2mm}
\caption{Overall Subscription Workflow.
(1)~Upon receiving a promotion, the consumer issues new sample tasks and requests fresh validators to assess the producer's capability.
(2)~Validators independently produce their outputs and reach a consensus.
(3)~The producer provides its own output to the same tasks.
(4)~Validators return the consensus result.
(5)~The consumer compares the two outputs and decides whether to subscribe to the service.}
\label{fig:subscription}
\end{figure*}


\subsubsection{Consensus Formation and Proof-of-Intelligence}
\label{sec:poi_detail}

To improve routing transparency and auditability, the ecosystem employs a decentralized consensus formation process where heterogeneous validators collaborate to deliver a more reliable judgment than any individual could provide. Upon a validation request, the selected committee acts as a decentralized proxy to verify producer-consumer alignment by independently invoking its routing services and comparing outputs with the producer's claims. These individual judgments are then aggregated via majority voting into an auditable consensus signal, which is returned to the requester as a formal reference for the final routing decision. This process is formalized for both stages below.

\begin{definition}{\textbf{(Promotion Consensus)}}\label{def:promo_consensus}
Let $V_p = \{v_1, \ldots, v_n\}$ denote the validator committee for promotion, where $n \ll N$. Each validator $v_i \in V_p$ independently invokes its routing service to produce a judgment $r_i = a_i(\mathcal{Q}_c, a_p)$ by comparing its own outputs with those of the candidate producer on the sample task set $\mathcal{Q}_c$. A promotion consensus $r^*$ is derived through majority voting:
\begin{equation}
r^* = \textup{MajorityVote}\left(\{r_1, \dots, r_n\}\right).
\end{equation}
The producer may adopt $r^*$ as a reference to decide whether to proceed with the promotion.
\end{definition}

\begin{definition}{\textbf{(Subscription Consensus)}}\label{def:sub_consensus}
Let $V_s = \{v_1', \ldots, v_n'\}$ be a disjoint committee of validators assigned to the subscription phase. To verify service alignment, each validator $v_j' \in V_s$ evaluates the producer’s responses on another specific task set $\mathcal{Q}_s$, yielding an independent judgment $r_j'$. The collective consensus $r^{**}$ is aggregated through majority voting:
\begin{equation}
r^{**} = \textup{MajorityVote}\left(\{r_1', \ldots, r_n'\}\right).
\end{equation}
The consumer may use $r^{**}$ as a reference signal to decide whether the producer's service is sufficiently aligned with its domain requirements.
\end{definition}

Ultimately, the requester, whether producer or consumer, retains full autonomy, choosing to follow or ignore the consensus based on its utility goals and risk appetite. However, once a subscription is confirmed, the producer agent becomes obligated to fulfill future service requests under a token-backed subscription commitment $\tau_s$.

We refer to this validator-mediated, majority-vote, and autonomy-preserving consensus formation process as the \textbf{Proof-of-Intelligence (PoI)} paradigm. While Proof-of-Work (PoW)~\cite{jakobsson1999proofs} requires raw computation and Proof-of-Stake (PoS)~\cite{dwork1992pricing} requires financial capital, PoI demands the primary currency in the agent society: domain-specific expertise and verifiable intellectual effort. By delegating quality assessment to a committee of economically motivated validators, PoI transforms a problem that no individual consumer or centralized orchestrator could resolve into a collective resolution, through incentive-aligned collaboration among distributed intelligences (see \S\ref{sec:tokenomics_loop}).

The efficacy of the PoI paradigm rests on two pillars: auditable protocols and incentive mechanism that ensure truthful and active participation, and the inherent computational intelligence of the agents themselves. Beyond mere participation, however, reliable consensus formation requires a critical prerequisite: \textbf{agent heterogeneity}. This diversity operates across multiple dimensions, requiring validators to differ not only in their base model architectures, such as a Llama-based agent auditing a GPT-based producer, but also in the training data, retrieval resources, and reasoning configurations that constitute their agent intelligence. Without such variance, validators sharing identical biases would exhibit highly correlated errors, collapsing the majority vote into a single point of failure.


\subsection{Tokenomics Incentive Structure}
\label{sec:tokenomics_loop}

The long-term viability of the agent ecosystem hinges not on the goodwill of participants but on a calibrated tokenomics structure that renders honest, effortful participation the dominant economic strategy. By encompassing staking, rewards, and penalties across all three agent roles, this framework ensures that economic self-interest aligns with system-wide integrity. We first detail the staking-based mechanism that governs validator behavior in each validation round (\S\ref{sec:staking}), then describe how the resulting token flows aggregate into the self-reinforcing dual loop that sustains the ecosystem (\S\ref{sec:dual_loop}), and finally consolidate the complete protocol into a single algorithmic specification (\S\ref{sec:protocol_summary}).

\subsubsection{Staking, Rewards, and Penalties}
\label{sec:staking}

The tokenomics structure binds all three agent roles through complementary token commitments: producer agents stake the promotion fee $\tau_p$ to enter the market, consumer agents commit the subscription fee $\tau_s$ to engage a chosen producer, and validator agents stake $\sigma$ tokens to participate in each validation round. We focus here on the validator-side mechanism, since it forms the economic backbone of the consensus protocol developed in \Cref{sec:poi_detail}.

Each validator agent participating in a validation round must lock $\sigma$ tokens as an economic commitment, creating direct financial exposure that deters dishonest behavior. Once voting concludes, a validator is rewarded if its judgment aligns with the consensus ($r_i = r^*$ in promotion or $r_j' = r^{**}$ in subscription), and penalized otherwise. The total reward pool consists of two sources: the requester's validation payment ($mn\tau_{\text{val}}$) and all validators' stakes ($n\sigma$). Letting $n_{\text{agree}}$ denote the number of validators whose judgments agree with the consensus, the per-capita payoff is:
\begin{equation}
\label{eq:validator_payoff_mechanism}
U_v =
\begin{cases}
\displaystyle\frac{mn\tau_{\text{val}} + n\sigma}{n_{\text{agree}}} - \sigma & \text{if } r_v = r^* \text{ (agree with consensus)}, \\[8pt]
-\sigma & \text{if } r_v \neq r^* \text{ (disagree with consensus)}.
\end{cases}
\end{equation}
In the conservative case where all $n$ validators agree ($n_{\text{agree}} = n$), the per-capita reward reduces to $R_{\min} = m\tau_{\text{val}} + \sigma$, which serves as the lower bound used in our game-theoretic analysis (\Cref{sec:ic}).

This staking-and-reward structure gives rise to several important properties of the validation mechanism:

\begin{itemize}[leftmargin=1.5em, itemsep=4pt]
\item \textbf{Sybil defense.} The staking requirement directly addresses the accountability crisis identified in \Cref{sec:trust_crisis}. While deploying a new agent is virtually free, staking tokens on that agent is not: an attacker seeking to dominate the consensus with $k$ Sybil validators must lock $k\sigma$ tokens, an investment that scales linearly with the attack's scope and is forfeited upon failure, effectively replacing disposable identity with costly economic commitment.

\item \textbf{Collusion resistance.} Strategic validator selection is mitigated by the disjoint committee design ($V_s \cap V_p = \varnothing$): even if the producer agent selects favorable validators for its promotion stage, the consumer agent independently re-validates with a separate committee before committing to subscription.

\item \textbf{Moral hazard.} Despite the staking mechanism, validator agents face a persistent temptation to shirk by returning random judgments at zero cost rather than exerting genuine effort at cost $c_u$. Resolving this moral hazard requires formal proof that truthful effort is each validator's optimal strategy, which we provide in \Cref{sec:ic}.

\item \textbf{Utility-based acceptance.} The requesting agent does not blindly follow the consensus: producer agents promote and consumer agents subscribe only when their expected utility exceeds the token-backed commitment, yielding threshold-optimal policies formalized in \Cref{sec:producer_ir,sec:consumer_ir}.
\end{itemize}

\subsubsection{Token Flow and the Tokenomics--Knowledge Dual Loop}
\label{sec:dual_loop}

While \S\ref{sec:staking} focused on the validator-side mechanism, the ecosystem's economic vitality ultimately depends on how tokens circulate among all three agent roles over time. As illustrated in Figure~\ref{fig:tokenomics}, our framework deliberately couples each role with both token-earning and token-spending pathways, thereby fostering a dynamic flow of value rather than a static one-shot transaction. Producer agents earn subscription fees from consumers but spend tokens on promotion stakes and validation requests; consumer agents pay subscription fees but receive promotion stakes from producers competing for their attention; validator agents earn validation fees and reward shares while staking $\sigma$ tokens against dishonest reporting. Table~\ref{tab:token_flow} summarizes the net token change for every (role, action) pair.

\begin{table}[!htbp]
\centering
\caption{Token flow across ecosystem actions. Each cell shows the net token change for a given agent role. Validator payoffs depend on consensus alignment, where $n_{\text{agree}}$ denotes the number of validators whose judgments agree with the majority.}
\vspace{2mm}
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.25}
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Agent Role} & \textbf{Promotion} & \textbf{Subscription} & \textbf{Promo.\ Validation} & \textbf{Sub.\ Validation} \\
\midrule
Producer agent & $-\tau_p$ & $+\tau_s$ & $-mn\tau_{\text{val}}$ & --- \\
\midrule
Consumer agent & $+\tau_p$ & $-\tau_s$ & --- & $-mn\tau_{\text{val}}$ \\
\midrule
Validator agent (agree) & --- & --- & \multicolumn{2}{c}{$+\dfrac{mn\tau_{\text{val}}+n\sigma}{n_{\text{agree}}} - \sigma$} \\[4pt]
Validator agent (disagree) & --- & --- & \multicolumn{2}{c}{$-\sigma$} \\
\bottomrule
\end{tabular}
\label{tab:token_flow}
\end{table}

The significance of this dual coupling becomes clear when viewed dynamically. An agent that only earned tokens would soon find its accumulated wealth economically inert, while an agent that only spent would quickly exhaust its reserves. By making every role both a beneficiary and a contributor of tokens, the ecosystem incentivizes agents to participate continuously across various contexts, shifting fluidly between producer, consumer, and validator depending on each agent's expertise advantage. This continuous circulation of value gives rise to what we call the \textbf{tokenomics--knowledge dual loop}, a self-reinforcing feedback cycle that operates in two directions. On the positive side, agents that deliver high-quality outputs and truthful assessments earn token rewards, and the resulting accumulation expands their participation opportunities across all three roles, reinforcing both their economic standing and the ecosystem's reliance on their capabilities. On the negative side, agents that deliver low-quality outputs or shirk effort see their token reserves deplete through failed validations and forfeited stakes, and are progressively filtered out of the active population. The two directions together produce an evolutionary selection pressure that continuously elevates the aggregate intelligence of the agent society.


\subsubsection{Protocol Summary}
\label{sec:protocol_summary}

We now consolidate the validation protocol of \Cref{sec:poi_detail} together with the tokenomics structure above into the complete agent service routing protocol. The protocol operates over a qualified validator pool $\mathcal{V} \subset \mathcal{U}$, where each member is registered with the ecosystem along with its expertise profile and stake balance. Algorithm~\ref{alg:ecosystem} traces the full lifecycle, from initial need identification, through validator-mediated quality assessment, to confirmed subscription.

\begin{algorithm}[!htbp]
\caption{Agent Service Routing Protocol}
\label{alg:ecosystem}
\begin{algorithmic}[1]
\Require Qualified validator pool $\mathcal{V} \subset \mathcal{U}$; sample size $m$; committee size $n$; fees $\tau_p, \tau_s, \tau_{\text{val}}$; validator stake $\sigma$
\Ensure A confirmed service engagement between consumer $c$ and producer $p$, or termination
\Statex
\Statex \textbf{Phase 1: Service Need Identification}
\State Consumer agent $c$ identifies a task outside its expertise and the target expertise domain
\State $c$ posts a service request with a sample task set $\mathcal{Q}_c$, where $|\mathcal{Q}_c| = m$
\Statex
\Statex \textbf{Phase 2: Promotion Stage}
\State Interested producer agent $p$ commits promotion fee $\tau_p$ to enter the competition
\State $V_p \gets \textsc{SampleCommittee}(\mathcal{V}; n, \text{exclude}=\{p\})$ \Comment{enforces heterogeneity from $p$}
\State $p$ pays $\tau_{\text{val}}$ per task to each $v_i \in V_p$
\For{each $v_i \in V_p$ \textbf{in parallel}}
    \State $v_i$ locks stake $\sigma$
    \State $v_i$ executes its routing service $a_{v_i}(x)$ for each $x \in \mathcal{Q}_c$
    \State $v_i$ submits a binary judgment $r_i \in \{0,1\}$ indicating whether its outputs agree with those of $a_p$
\EndFor
\State $r^* \gets \textsc{MajorityVote}(\{r_1, \dots, r_n\})$
\State $n_{\text{agree}} \gets |\{i : r_i = r^*\}|$
\For{each $v_i \in V_p$} \Comment{settlement; cf.\ Eq.~\eqref{eq:validator_payoff_mechanism}}
    \If{$r_i = r^*$}
        \State $v_i$ receives reward $\dfrac{mn\tau_{\text{val}} + n\sigma}{n_{\text{agree}}} - \sigma$
    \Else
        \State $v_i$ forfeits stake $\sigma$
    \EndIf
\EndFor
\If{$r^* = 1$ \textbf{and} producer $p$'s expected payoff justifies promotion} \Comment{IR condition; see \S\ref{sec:producer_ir}}
    \State $p$ submits a promotion offer to $c$
\Else
    \State \textbf{terminate}
\EndIf
\Statex
\Statex \textbf{Phase 3: Subscription Stage}
\State $c$ posts a fresh task set $\mathcal{Q}_s$ such that $\mathcal{Q}_s \cap \mathcal{Q}_c = \varnothing$
\State $V_s \gets \textsc{SampleCommittee}(\mathcal{V}; n, \text{exclude}=V_p \cup \{p\})$ \Comment{disjoint and heterogeneous}
\State $c$ pays $\tau_{\text{val}}$ per task to each $v_j' \in V_s$
\For{each $v_j' \in V_s$ \textbf{in parallel}}
    \State $v_j'$ locks stake $\sigma$
    \State $v_j'$ executes its routing service $a_{v_j'}(x)$ for each $x \in \mathcal{Q}_s$
    \State $v_j'$ submits a binary judgment $r_j' \in \{0,1\}$ indicating whether its outputs agree with those of $a_p$
\EndFor
\State $r^{**} \gets \textsc{MajorityVote}(\{r_1', \dots, r_n'\})$
\State $n_{\text{agree}} \gets |\{j : r_j' = r^{**}\}|$
\For{each $v_j' \in V_s$} \Comment{settlement, analogous to Phase 2}
    \If{$r_j' = r^{**}$}
        \State $v_j'$ receives reward $\dfrac{mn\tau_{\text{val}} + n\sigma}{n_{\text{agree}}} - \sigma$
    \Else
        \State $v_j'$ forfeits stake $\sigma$
    \EndIf
\EndFor
\If{$r^{**} = 1$ \textbf{and} consumer $c$'s expected utility justifies subscription} \Comment{IR condition; see \S\ref{sec:consumer_ir}}
    \State $c$ commits subscription fee $\tau_s$ and enters a long-term service engagement with $p$
\Else
    \State \textbf{terminate}
\EndIf
\end{algorithmic}
\end{algorithm}

The two phases of Algorithm~\ref{alg:ecosystem} share a common validation substructure: in each phase, the validator for-loop and the subsequent settlement loop together implement the same five-step routine that defines Proof-of-Intelligence in operation.

\begin{insightbox}{Validation Lifecycle (Proof-of-Intelligence)}
\begin{enumerate}[leftmargin=1.5em, itemsep=1pt, topsep=2pt]
    \item \textbf{Stake}: Each validator agent locks $\sigma$ tokens as an economic commitment to the ecosystem.
    \item \textbf{Infer}: Each validator independently executes its routing service $a_v(x)$ on the sample tasks, drawing on its own expertise.
    \item \textbf{Compare}: Each validator compares its output against the producer's output $a_p(x)$, producing a binary judgment $r_v \in \{0,1\}$.
    \item \textbf{Vote}: Individual judgments are aggregated via majority voting to form a consensus signal.
    \item \textbf{Settle}: Validators aligned with consensus share the reward pool; those who deviate forfeit their stake.
\end{enumerate}
\end{insightbox}

The mechanism design developed across \S\ref{sec:motivation}--\S\ref{sec:protocol_summary} thus restores cooperation, accountability, and quality verification, the three properties whose simultaneous breakdown defines the trust crisis of \Cref{sec:trust_crisis}, as emergent consequences of rational self-interest rather than external enforcement. A critical enabler is that LLM agents, unlike human participants subject to bounded rationality and social preferences, are programmatic utility maximizers whose behavior can be precisely modeled as optimal responses to incentive structures. This makes the game-theoretic predictions underlying our mechanism design, which we formalize in \Cref{sec:game_theory}, hold with particular force in the agent society context.


\begin{remarkbox}{Implementation Considerations}
\label{sec:decentral_tech}
The mechanism design above is infrastructure-agnostic in principle, but its trustless execution naturally maps onto blockchain-based infrastructure. In the agent society, where participants are deployed by independent, competing organizations with no pre-existing trust relationships, no single entity can serve as a credible neutral party. A shared ledger enforced by network consensus provides the necessary trust layer for token management, stake escrow, and automated reward distribution via smart contracts, while a hybrid on-chain/off-chain architecture preserves the data sovereignty that cross-organizational agents require (protocol state on-chain; proprietary knowledge assets off-chain with cryptographic integrity proofs). We leave the full system implementation as future work and focus this paper on the mechanism design, incentive analysis, and empirical validation.
\end{remarkbox}
